<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>org.springframework.boot</groupId>
    <artifactId>spring-boot-starter-parent</artifactId>
    <version>4.1.0</version>
    <relativePath/>
  </parent>

  <groupId>com.bovae.yaj</groupId>
  <artifactId>yet-another-jira-backend</artifactId>
  <version>0.0.1-SNAPSHOT</version>
  <name>yet-another-jira-backend</name>
  <description>yet-another-jira application skeleton backend (application tier)</description>

  <properties>
    <java.version>21</java.version>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

    <bouncycastle.version>1.84</bouncycastle.version>
    <mapstruct.version>1.6.3</mapstruct.version>
    <lombok-mapstruct-binding.version>0.2.0</lombok-mapstruct-binding.version>
    <cucumber.version>7.18.1</cucumber.version>
    <jjwt.version>0.12.6</jjwt.version>

    <error-prone.version>2.38.0</error-prone.version>
    <nullaway.version>0.12.7</nullaway.version>
    <errorprone.skip>false</errorprone.skip>

    <build-helper-maven-plugin.version>3.6.1</build-helper-maven-plugin.version>
    <jacoco-maven-plugin.version>0.8.13</jacoco-maven-plugin.version>
    <spotless-maven-plugin.version>3.5.1</spotless-maven-plugin.version>
    <palantir-java-format.version>2.71.0</palantir-java-format.version>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-web</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-security</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-actuator</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-validation</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-redis</artifactId>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-mail</artifactId>
    </dependency>

    <dependency>
      <groupId>org.apache.commons</groupId>
      <artifactId>commons-lang3</artifactId>
    </dependency>
    <dependency>
      <groupId>org.bouncycastle</groupId>
      <artifactId>bcprov-jdk18on</artifactId>
      <version>${bouncycastle.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-api</artifactId>
      <version>${jjwt.version}</version>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-impl</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>io.jsonwebtoken</groupId>
      <artifactId>jjwt-jackson</artifactId>
      <version>${jjwt.version}</version>
      <scope>runtime</scope>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <scope>runtime</scope>
    </dependency>
    <dependency>
      <groupId>org.liquibase</groupId>
      <artifactId>liquibase-core</artifactId>
    </dependency>

    <dependency>
      <groupId>org.projectlombok</groupId>
      <artifactId>lombok</artifactId>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.mapstruct</groupId>
      <artifactId>mapstruct</artifactId>
      <version>${mapstruct.version}</version>
      <scope>provided</scope>
      <optional>true</optional>
    </dependency>

    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-maven-plugin</artifactId>
        <configuration>
          <excludes>
            <exclude>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
            </exclude>
            <exclude>
              <groupId>org.mapstruct</groupId>
              <artifactId>mapstruct</artifactId>
            </exclude>
            <exclude>
              <groupId>org.springframework.boot</groupId>
              <artifactId>spring-boot-devtools</artifactId>
            </exclude>
          </excludes>
        </configuration>
      </plugin>

      <!-- Attach Byte Buddy as a proper Java agent so Mockito's inline-mock-maker does not
           rely on dynamic agent attachment (blocked by default in future JDK releases). -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <argLine>@{argLine} -javaagent:${settings.localRepository}/net/bytebuddy/byte-buddy-agent/${byte-buddy.version}/byte-buddy-agent-${byte-buddy.version}.jar</argLine>
        </configuration>
      </plugin>

      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <version>${jacoco-maven-plugin.version}</version>
        <executions>
          <!-- Instrument the test JVM. Unit tests always run under surefire; when -Pbdd is active
               the Cucumber suite (a *Test) runs under surefire too, so unit + BDD coverage
               accumulate into a single jacoco.exec. The 90% gate lives in the bdd profile because
               it needs that combined data (a plain `verify` would only have unit coverage). -->
          <execution>
            <id>prepare-agent</id>
            <goals>
              <goal>prepare-agent</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <release>${java.version}</release>
          <compilerArgs>
            <arg>-XDcompilePolicy=simple</arg>
            <arg>--should-stop=ifError=FLOW</arg>
            <!-- Required on JDK 21 (google/error-prone#5426). -->
            <arg>-XDaddTypeAnnotationsToSymbol=true</arg>
            <arg>-Xplugin:ErrorProne -Xep:NullAway:ERROR -XepOpt:NullAway:AnnotatedPackages=com.bovae.yaj -XepExcludedPaths:.*/src/(test|bdd|it)/java/.*</arg>
          </compilerArgs>
          <annotationProcessorPaths>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok</artifactId>
              <version>${lombok.version}</version>
            </path>
            <path>
              <groupId>org.mapstruct</groupId>
              <artifactId>mapstruct-processor</artifactId>
              <version>${mapstruct.version}</version>
            </path>
            <path>
              <groupId>org.projectlombok</groupId>
              <artifactId>lombok-mapstruct-binding</artifactId>
              <version>${lombok-mapstruct-binding.version}</version>
            </path>
            <path>
              <groupId>com.google.errorprone</groupId>
              <artifactId>error_prone_core</artifactId>
              <version>${error-prone.version}</version>
            </path>
            <path>
              <groupId>com.uber.nullaway</groupId>
              <artifactId>nullaway</artifactId>
              <version>${nullaway.version}</version>
            </path>
          </annotationProcessorPaths>
        </configuration>
      </plugin>

      <plugin>
        <groupId>com.diffplug.spotless</groupId>
        <artifactId>spotless-maven-plugin</artifactId>
        <version>${spotless-maven-plugin.version}</version>
        <configuration>
          <java>
            <includes>
              <include>src/main/java/**/*.java</include>
              <include>src/test/java/**/*.java</include>
              <include>src/bdd/java/**/*.java</include>
              <include>src/it/java/**/*.java</include>
            </includes>
            <palantirJavaFormat>
              <version>${palantir-java-format.version}</version>
            </palantirJavaFormat>
            <importOrder/>
            <removeUnusedImports/>
            <forbidWildcardImports/>
          </java>
          <gherkin>
            <includes>
              <include>src/**/*.feature</include>
            </includes>
            <gherkinUtils/>
          </gherkin>
          <pom>
            <includes>
              <include>pom.xml</include>
            </includes>
            <sortPom/>
          </pom>
        </configuration>
        <executions>
          <execution>
            <id>spotless-check</id>
            <goals>
              <goal>check</goal>
            </goals>
          </execution>
        </executions>
      </plugin>

      <!-- Thin-jar enforcement: after repackage (declared last so it runs after),
           fail the build if Lombok/MapStruct/Testcontainers/Cucumber/devtools classes
           or jars leak into BOOT-INF of the artifact. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-antrun-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-thin-jar</id>
            <goals>
              <goal>run</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <target>
                <property name="thinjar.file" value="${project.build.directory}/${project.build.finalName}.jar"/>

                <fail message="Thin-jar violation: 'lombok' classes/jar leaked into ${thinjar.file}. Lombok must remain a compile-time-only (provided/optional) annotation processor and must not be packaged.">
                  <condition>
                    <resourcecount count="0" when="greater">
                      <zipfileset includes="BOOT-INF/lib/lombok-*.jar,BOOT-INF/classes/lombok/**" src="${thinjar.file}"/>
                    </resourcecount>
                  </condition>
                </fail>

                <fail message="Thin-jar violation: 'mapstruct' classes/jar leaked into ${thinjar.file}. MapStruct must remain a compile-time-only (provided/optional) annotation processor and must not be packaged.">
                  <condition>
                    <resourcecount count="0" when="greater">
                      <zipfileset includes="BOOT-INF/lib/mapstruct-*.jar,BOOT-INF/classes/org/mapstruct/**" src="${thinjar.file}"/>
                    </resourcecount>
                  </condition>
                </fail>

                <fail message="Thin-jar violation: 'testcontainers' classes/jar leaked into ${thinjar.file}. Testcontainers must remain test-scoped and must not be packaged.">
                  <condition>
                    <resourcecount count="0" when="greater">
                      <zipfileset includes="BOOT-INF/lib/testcontainers-*.jar,BOOT-INF/lib/*-testcontainers-*.jar,BOOT-INF/classes/org/testcontainers/**" src="${thinjar.file}"/>
                    </resourcecount>
                  </condition>
                </fail>

                <fail message="Thin-jar violation: 'cucumber' classes/jar leaked into ${thinjar.file}. Cucumber must remain test-scoped and must not be packaged.">
                  <condition>
                    <resourcecount count="0" when="greater">
                      <zipfileset includes="BOOT-INF/lib/cucumber-*.jar,BOOT-INF/classes/io/cucumber/**" src="${thinjar.file}"/>
                    </resourcecount>
                  </condition>
                </fail>

                <fail message="Thin-jar violation: 'devtools' classes/jar leaked into ${thinjar.file}. spring-boot-devtools must remain behind the opt-in 'dev' profile and must not be packaged.">
                  <condition>
                    <resourcecount count="0" when="greater">
                      <zipfileset includes="BOOT-INF/lib/spring-boot-devtools-*.jar,BOOT-INF/classes/org/springframework/boot/devtools/**" src="${thinjar.file}"/>
                    </resourcecount>
                  </condition>
                </fail>
              </target>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <!-- errorprone-skip: -Derrorprone.skip drops the -Xplugin:ErrorProne arg
         (combine.self="override" replaces the base compilerArgs) for a fast compile.
         The processor paths stay present but inert. -->
    <profile>
      <id>errorprone-skip</id>
      <activation>
        <property>
          <name>errorprone.skip</name>
        </property>
      </activation>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-compiler-plugin</artifactId>
            <configuration>
              <compilerArgs combine.self="override"/>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!-- dev: opt-in spring-boot-devtools (optional so it never reaches the runtime jar). -->
    <profile>
      <id>dev</id>
      <dependencies>
        <dependency>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-devtools</artifactId>
          <scope>runtime</scope>
          <optional>true</optional>
        </dependency>
      </dependencies>
    </profile>

    <!-- it: Testcontainers (Postgres) integration-test support, test scope. -->
    <profile>
      <id>it</id>
      <dependencies>
        <dependency>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-testcontainers</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>junit-jupiter</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>postgresql</artifactId>
          <scope>test</scope>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <!-- Bind the dedicated integration-test source set: src/it/java. Keeps Testcontainers-
               dependent tests out of the always-compiled src/test/java so a profile-less
               `mvn test` still compiles and runs the fast unit tests. -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <version>${build-helper-maven-plugin.version}</version>
            <executions>
              <execution>
                <id>add-it-test-source</id>
                <goals>
                  <goal>add-test-source</goal>
                </goals>
                <phase>generate-test-sources</phase>
                <configuration>
                  <sources>
                    <source>src/it/java</source>
                  </sources>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>

    <!-- bdd: binds the src/bdd/java source set + Cucumber runner + Testcontainers (test scope). -->
    <profile>
      <id>bdd</id>
      <dependencies>
        <dependency>
          <groupId>io.cucumber</groupId>
          <artifactId>cucumber-java</artifactId>
          <version>${cucumber.version}</version>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>io.cucumber</groupId>
          <artifactId>cucumber-spring</artifactId>
          <version>${cucumber.version}</version>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>io.cucumber</groupId>
          <artifactId>cucumber-junit-platform-engine</artifactId>
          <version>${cucumber.version}</version>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.junit.platform</groupId>
          <artifactId>junit-platform-suite</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.springframework.boot</groupId>
          <artifactId>spring-boot-testcontainers</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>junit-jupiter</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>org.testcontainers</groupId>
          <artifactId>postgresql</artifactId>
          <scope>test</scope>
        </dependency>
        <dependency>
          <groupId>com.icegreen</groupId>
          <artifactId>greenmail</artifactId>
          <version>2.1.2</version>
          <scope>test</scope>
        </dependency>
      </dependencies>
      <build>
        <plugins>
          <!-- Isolate the Cucumber suite from the shared (reused-fork) surefire JVM.
               cucumber-spring's @CucumberContextConfiguration glue detection is sensitive to
               state left behind by other tests sharing a reused fork; running the suite in its
               own fork removes that ordering fragility so -Pbdd is reliable standalone, not only
               with -Pbdd,it. Both executions inherit the plugin-level JaCoCo agent argLine, so
               BDD coverage still appends into the shared jacoco.exec the 90% gate reads. -->
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <executions>
              <!-- Keep CucumberSuiteTest out of the default (reused-fork) run. -->
              <execution>
                <id>default-test</id>
                <configuration>
                  <excludes>
                    <exclude>**/CucumberSuiteTest.java</exclude>
                  </excludes>
                </configuration>
              </execution>
              <!-- Run only CucumberSuiteTest, in a fresh fork of its own. -->
              <execution>
                <id>bdd-test</id>
                <goals>
                  <goal>test</goal>
                </goals>
                <phase>test</phase>
                <configuration>
                  <includes>
                    <include>**/CucumberSuiteTest.java</include>
                  </includes>
                  <reuseForks>false</reuseForks>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <!-- Bind the dedicated BDD source set: src/bdd/java for
                         glue/runner code and src/bdd/resources for features. -->
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>build-helper-maven-plugin</artifactId>
            <version>${build-helper-maven-plugin.version}</version>
            <executions>
              <execution>
                <id>add-bdd-test-source</id>
                <goals>
                  <goal>add-test-source</goal>
                </goals>
                <phase>generate-test-sources</phase>
                <configuration>
                  <sources>
                    <source>src/bdd/java</source>
                  </sources>
                </configuration>
              </execution>
              <execution>
                <id>add-bdd-test-resource</id>
                <goals>
                  <goal>add-test-resource</goal>
                </goals>
                <phase>generate-test-resources</phase>
                <configuration>
                  <resources>
                    <resource>
                      <directory>src/bdd/resources</directory>
                    </resource>
                  </resources>
                </configuration>
              </execution>
            </executions>
          </plugin>
          <!-- JaCoCo coverage gate. Bound here rather than the main build because it needs the
                         combined unit + BDD coverage: this profile adds the src/bdd source set whose
                         Cucumber suite runs under surefire into the same jacoco.exec the main-build
                         agent produces. -->
          <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>${jacoco-maven-plugin.version}</version>
            <executions>
              <execution>
                <id>check-coverage</id>
                <goals>
                  <goal>check</goal>
                </goals>
                <phase>verify</phase>
                <configuration>
                  <rules>
                    <rule>
                      <element>BUNDLE</element>
                      <limits>
                        <limit>
                          <counter>LINE</counter>
                          <value>COVEREDRATIO</value>
                          <minimum>0.90</minimum>
                        </limit>
                        <limit>
                          <counter>BRANCH</counter>
                          <value>COVEREDRATIO</value>
                          <minimum>0.90</minimum>
                        </limit>
                      </limits>
                    </rule>
                  </rules>
                  <excludes>
                    <exclude>**/*Application.*</exclude>
                    <exclude>**/config/*Config.*</exclude>
                    <exclude>**/mapper/*Impl*</exclude>
                    <exclude>**/model/**</exclude>
                  </excludes>
                </configuration>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
